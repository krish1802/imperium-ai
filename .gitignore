# ── Secrets — NEVER commit these ─────────────────────────────────────────
.streamlit/secrets.toml
*service_account*.json
*client_secret*.json
gsc_token.json
.env

# ── Generated data / reports ─────────────────────────────────────────────
# The dashboard reads report CSVs, and the GitHub Actions job commits them,
# so ALLOW the traffic CSVs while ignoring everything else generated.
reports/
seo_reports/**
!seo_reports/
!seo_reports/**/
!seo_reports/**/traffic_generated_*.csv

# ── Python ───────────────────────────────────────────────────────────────
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/
